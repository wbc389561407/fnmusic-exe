# fnmusic-exe · 飞牛音乐客户端

> 基于 Electron 封装的飞牛音乐客户端，核心解决网页版无法后台播放、最小化后切歌中断的痛点。

项目来源：https://github.com/wbc389561407/fnmusic-exe

![img_1.png](img_1.png)

## 立项初衷

飞牛音乐官方早期仅提供网页版，存在两个让人难以忍受的问题：

1. **浏览器标签页切到后台后，音频会被节流甚至暂停**，导致切歌、自动播放下一首失效；
2. **关闭浏览器或清理 cookie 后登录态丢失**，需要反复扫码登录。

本项目就是为了给自己做一个能稳定后台播放、自动切歌的桌面客户端。本质上是用 Electron 套了一层壳，关掉后台节流、持久化登录态、加个托盘，让网页版"无感知"地当一个原生应用来用。

> **声明**：本项目为个人自用性质，仅供学习交流，请优先使用官方版本。

## 功能特性

- **后台不节流**：关闭 `backgroundThrottling`，窗口最小化 / 失焦后，页面定时器与音频仍正常推进，自动切歌不中断
- **登录态持久化**：基于 `persist:feiniu` 分区，cookies / localStorage 落盘到 `userData`，重启后免重新登录；会话型 cookie 自动续期 1 年
- **托盘常驻**：点叉叉 = 最小化到托盘，仅在托盘右键「退出」时真正关闭
- **无边框外观**：隐藏原生标题栏，右上角提供置顶 / 设置 / 用户菜单 / 最小化 / 最大化/还原 / 关闭按钮，顶部注入可拖拽区域
- **窗口自适应缩放**：拖动改变窗口尺寸时页面按宽度等比缩放，吸附到固定缩放档位保证字体清晰
- **任务栏缩略图控制**：鼠标悬停 Windows 任务栏图标，可直接上一首 / 播放暂停 / 下一首
- **服务器地址可配置**：首次启动进入设置页输入服务器地址；菜单栏「设置 → 切换服务器地址」可随时切换
- **站内导航限制**：仅允许停留在已配置的服务器站内，外链自动转交系统默认浏览器打开
- **UA 伪装**：伪装为普通 Chrome 浏览器，避免被站点拦截
- **NSIS 安装包**：支持桌面快捷方式、开始菜单、自定义安装路径

## 安装与使用

### 方式一：直接安装（推荐）

下载 [Releases](../../releases) 中的 `飞牛音乐-Setup-x.x.x.exe`，双击安装即可。

首次启动会进入服务器地址输入页，填入你部署的飞牛音乐服务地址后进入主界面。

### 方式二：从源码运行

```bash
# 安装依赖
npm install

# 开发模式启动
npm start

# 打包 Windows 安装包
npm run dist

# 打包 portable 免安装版
npm run dist:portable
```

> 运行前需自行配置好 Node.js 环境与 Electron 依赖。

## 常见操作

| 操作 | 行为 |
| --- | --- |
| 点击窗口右上角 × | 最小化到托盘，不退出 |
| 点击窗口右上角 📌 置顶按钮 | 窗口置顶 / 取消置顶，置顶时按钮高亮 |
| 点击窗口右上角 ─ / ▢ | 最小化 / 最大化（再点还原） |
| 悬停 Windows 任务栏图标 | 显示上一首 / 播放暂停 / 下一首控制按钮 |
| 拖动调整窗口大小 | 页面按宽度自适应等比缩放 |
| 单击托盘图标 | 显示 / 聚焦主窗口 |
| 托盘右键 → 显示主窗口 | 显示主窗口 |
| 托盘右键 → 退出飞牛音乐 | 真正退出应用 |
| 菜单栏 → 设置 → 切换服务器地址 | 回到设置页重新填写地址 |
| 菜单栏 → 设置 → 清除已保存地址并重置 | 删除配置文件并重置 |

## 配置文件位置

配置文件 `config.json` 与 cookie 存储位于 Electron 的 `userData` 目录：

- Windows: `%APPDATA%\feiniu-music\`

## 技术栈

- Electron 28
- electron-builder 24（NSIS 打包）
- 原生 JavaScript，无前端框架

## 版本号

当前版本：**v2.5.3**

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)（Semantic Versioning）：

- **主版本号（MAJOR）**：存在不兼容的 API / 行为变更时递增
- **次版本号（MINOR）**：新增向下兼容的功能时递增
- **修订号（PATCH）**：仅做向下兼容的缺陷修复时递增

版本号同步维护在 [package.json](package.json) 的 `version` 字段。

## 更新日志

### v2.5.3

- 统一菜单栏配色
- 播放器栏目贴底

### v2.5.2

- 自定义标题栏新增四个窗口控制按钮：置顶、最小化、最大化/还原、关闭
  - 置顶：点击后窗口始终保持在最前，再次点击取消，置顶时按钮高亮显示
  - 最大化/还原：图标随窗口状态自动切换
  - 关闭：行为不变，仍为最小化到托盘
- 窗口尺寸变化时页面自适应缩放：以 1280 宽为 100% 基准等比缩放，吸附到浏览器固定缩放档位，字体清晰不模糊
- 标题栏拖拽条背景改为透明，窗口底色改为与页面一致的深色（`#1f1f2e`），顶部与整体颜色统一无断层
- 修复底部歌曲列表遮挡：列表最后一首歌曲追加底部内边距，可完整滚动至可见
- 新增 Windows 任务栏缩略图控制：鼠标悬停任务栏图标显示上一首、播放/暂停、下一首按钮，图标随播放状态实时切换
- 标题栏新增「设置」「用户菜单」按钮（设置齿轮、个人图标），页面自带的设置/用户悬浮胶囊同步隐藏，点击弹出菜单与原页面一致

### v2.5.1

- 修复：地址输入错误或飞牛 NAS 未安装飞牛音乐时（如返回 502），直接加载错误页导致「连接中」卡死的问题
- 新增连接前地址校验：先请求目标地址验证是否为飞牛音乐服务，校验失败立即提示，按钮恢复可点击
- 校验失败按场景给出针对性提示：
  - 502 / 5xx → 提示飞牛 NAS 上未安装或未启动飞牛音乐
  - 404 / 403 → 提示未检测到飞牛音乐服务
  - 页面无飞牛音乐特征 → 提示该地址不是飞牛音乐服务
  - 网络不通 / 超时 / 证书错误 → 分别给出对应提示
- 启动时同样校验已保存的地址，失败自动回到设置页并显示原因
- 远程页面主框架加载失败时自动跳回设置页并提示，不再卡在错误页 / 白屏

### v2.5.0

- 启动后自动进入的页面改为可配置，不再固定为「音乐库」
- 托盘右键菜单新增「启动页」子菜单，可选择：首页、收藏、最近、专辑、歌手、风格、音乐库
- 自定义歌单自动从侧边栏读取并列入「启动页」子菜单，选中后启动时自动进入对应歌单
- 默认启动页改为「首页」（不点击任何导航项，保持站点默认行为）
- 托盘「打开自动播放」升级为「自动播放」子菜单，分为：关闭、继续播放、全部播放、随机播放
  - 继续播放：点击底部播放按钮（原功能）
  - 全部播放：启动页为歌单时点击「播放全部」，为音乐库时点击「随机漫游」
  - 随机播放：启动页为歌单时点击「随机」，为音乐库时点击「随机漫游」

### v2.4.0

- 托盘「重置地址」改为「退出登录」，仅清除登录态（cookie 与密码），保留服务器地址和用户名
- 设置页用户名、密码改为必填，退出登录后自动回填已保存的地址与用户名
- 登录接口返回错误时立即跳回设置页并提示「用户名或密码错误」，无需等待超时
- 启动登录成功后自动进入音乐库页面
- 隐藏首页「最近添加歌曲」模块（保留「最近添加专辑」）
- 浏览器缩放比例固定为 100%

### v2.3.4

- 新增登录失败自动处理：用户名密码错误时自动跳回设置页并提示，不再卡在登录页
- 隐藏页面内「退出登录」入口，退出操作统一由托盘菜单管理，避免误触退出
- 未保存登录凭据时自动跳回设置页，引导用户重新填写

### v2.3.0

- 新增登录态失效后自动重新登录：cookie 过期跳转到登录页时，自动填入已保存的用户名密码并提交
- 设置页新增用户名、密码输入框，登录凭据一并持久化保存
- 支持单页应用内路由切换时触发自动登录，无需整页刷新

### v2.2.1

- 新增三档窗口尺寸预设：大/中/小窗口，启动时按屏幕高度自动选择
- 托盘右键菜单新增「窗口大小」切换
- 页面缩放按窗口高度自动适配，字体清晰不模糊

### v2.2.0

- 右上角关闭按钮缩小并调灰，鼠标悬停只改变图标颜色不改变底色

### v2.1.9

- 新增启动时自动更新检测，延迟 3 秒异步执行，版本不一致时提示下载

### v2.1.8

- 新增启动时自动更新检测，版本不一致时提示下载地址

### v2.1.7

- 托盘右键新增「打开自动播放」选项，勾选后启动 app 自动播放

### v2.1.6

- 修复开机自启后出现额外窗口的问题

### v2.1.5

- 修复打包后设置页报「初始化失败：桥接对象不可用」的问题

### v2.1.4

- 设置页新增版本号显示
- 托盘右键新增「关于」项
- 安装包文件名改为 `fnmusic-Setup-x.x.x.exe`

### v2.1.0

- fnid 解析重构：恢复通过 fnos.net API 解析真实服务器地址
- 修复 fnid 在局域网跳出 app 框架的问题
- 优化跨域导航处理

### v2.0.0

- 修复普通 https 站点 SSL 握手被拒绝导致无法加载的问题
- 统一地址解析入口，支持 fnid / IP / 域名 / 完整地址
- fnid 改为直接走中继地址，移除 API 解析与并发探测

### v1.8.1

- 修复访问 `/music` 卡在「连接中」的问题

### v1.8.0

- 重构地址解析为统一入口

### v1.7.0

- 简化 fnid 访问逻辑，直接走中继地址

### v1.6.0

- 新增 fnid 登录支持，自动解析真实服务器地址
- 局域网 / 外网智能适配，优先局域网直连
- 放行 fnos.net 与 NAS IP 的 SSL 证书验证

### v1.5.4

- 服务器地址输入页新增项目来源链接
- 纯 IP 自动补默认端口
- 应用单实例锁，避免重复启动

### v1.5.3

- 地址不以 `/music` 结尾时自动补全后缀

### v1.5.2

- 不带协议的地址默认按 http 访问

### v1.5.1

- 窗口启动背景与叉叉按钮底色改为透明
- 修复底部播放控制栏溢出窗口边界的问题

### v1.5.0

- 服务器地址校验：必须以 `/music` 或 `/music/` 结尾

### v1.4.0

- 隐藏最小化 / 最大化按钮，仅保留叉叉
- 托盘右键新增「重置地址」

### v1.3.0

- 关闭后台节流，解决最小化后切歌中断
- 会话型 cookie 自动续期 1 年
- 叉叉改为最小化到托盘
- 首次启动进入服务器地址设置页
- 站内导航限制，外链转交系统浏览器
- UA 伪装为 Chrome 浏览器
- NSIS 安装包

### v1.2.0

- 持久化 cookies / localStorage
- 新增托盘图标与右键菜单
- 服务器地址配置文件化

### v1.1.0

- 基础无边框窗口外观
- 注入顶部可拖拽区域

### v1.0.0

- 项目立项
- 基于 Electron 封装飞牛音乐网页

## License

[MIT](LICENSE)
